import { PageContainer } from '@ant-design/pro-components';

const MedalPage: React.FC = () => {
  return (
    <PageContainer
      ghost
      header={{
        title: '我的勋章',
      }}
    ></PageContainer>
  );
};

export default MedalPage;
