import { PageContainer } from '@ant-design/pro-components';

const TracePage: React.FC = () => {
  return (
    <PageContainer
      ghost
      header={{
        title: '我的足迹',
      }}
    ></PageContainer>
  );
};

export default TracePage;
